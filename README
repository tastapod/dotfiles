# Clone this into your home directory, then symlink like this:
# (You'll  need to move your dotfiles out the way first).

for f in cvsignore gemrc gitconfig pythonstartup tmux.conf vimfiles/vim vimfiles/vimrc zshrc zshenv; do ln -s dotfiles/$f .$(basename $f); done
