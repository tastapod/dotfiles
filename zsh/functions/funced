function {
    if [[ $# != 1 ]]; then
        print "Usage: $0 funcname"
        return 1
    fi

    [[ -d "$ZSH_CUSTOM/functions" ]] || mkdir -p "$ZSH_CUSTOM/functions"

    ${EDITOR:-vim} "$ZSH_CUSTOM/functions/$1" && funcload
}
# vi: filetype=bash
